{
  "name": "OmService",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.thegraph.com/deploy/ ddevkim/OmService",
    "create-local": "graph create --node http://localhost:8020/ ddevkim/OmService",
    "remove-local": "graph remove --node http://localhost:8020/ ddevkim/OmService",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 ddevkim/OmService"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.26.0",
    "@graphprotocol/graph-ts": "0.24.1"
  }
}
